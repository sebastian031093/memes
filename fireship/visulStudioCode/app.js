let hello = '😅';
const noway = '🍖';

const hi = (hi, what) => `${hi} desde mi pc`;

(() => {
  /* ... */
})();

(async () => {
  /* ... */
})();

crypto.getRandomValues();
console.log();

function createHuman(sould, boddy) {}

class Animals {
  constructor(spcie, lifeYears) {
    this.spcie = spcie;
    this.lifeYears;
  }

  calcAge() {}
}

class Dog {
  dna = '🧬';
}
class Dog {
  dna = '🧬';
}
class Cat {
  dna = '🧬';
}
